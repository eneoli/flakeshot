#[derive(Default, Debug)]
pub struct Geometry {
    pub x: i32,
    pub y: i32,
    pub physical_width: i32,
    pub physical_height: i32,
    pub model: String,
    pub make: String,
    // transform
    // subpixel
}